// <!-- <h4>Create a color generator webpage. The page should generate a certain number of random color</h4>
    // <p>1. Create a function to generate a random color HEX code</p>
    // <p>2. Create a function to add an new color item to the list, passing the color code as arguement</p>
    // <p>3. Test that function by calling it with a random color</p>
    // <p>4. Create a function to handle the removing of an item, passing the event as argument</p>
    // <p>5. Register the event handler to catch the event and attach the handler(4)</p>
    // <p>6. Test the removal clicking on existing color itmems</p>
    // <p>7. Create a function to handle the submit of the form for adding more colors, which generates the needed amount of random hex colors and adds thme to the list</p>
    // <p>8. Register the submit event and attach the handler(7)</p>
    // <p>9. Create a function init() that is called on load and generates 3 color elments</p>

    // <p>See --Example once your program is running properly:</p> -->